# --- Development Dockerfile ---
# This image is used solely for running the Vite dev server with hot-reloading.
FROM node:lts-krypton

# Set the working directory
WORKDIR /app

# Install dependencies only (no need to copy all source code yet)
# We assume you now have package-lock.json since you switched to npm
COPY package*.json ./

# Use 'npm ci' for reliable, reproducible dependency installation
RUN npm ci

# The source code will be mounted from the host (your machine) via Docker Compose.
# We expose the default Vite dev server port.
EXPOSE 5173

# Default command: Run the Vite dev server.
# This command is often overridden by Docker Compose.
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]